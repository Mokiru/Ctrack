# LinkedList

`LinkedList` 是一个基于双向链表实现的集合类，经常被拿来和 `ArrayList` 做比较。

![alt text](image.png)

实际使用中，一般不会使用 `LinkedList` ，因为需要用到它的时候 `ArrayList` 都能够替代， 而不使用数组的时候又有其它的数据结构来替代它。换句话说，不要使用 `LinkedList`。

